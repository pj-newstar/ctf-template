version: '3.8'

services:
  challenge:
    image: ctf-nc-file-pow:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "11337:1337"
    environment:
      FLAG: "flag{this_is_a_sample_flag}"